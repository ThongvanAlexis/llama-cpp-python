---
phase: 01-scaffold-toolchain-pinning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/build-wheels-cuda-windows.yaml
autonomous: true
requirements: [WF-01, WF-02, WF-03, WF-04, TC-01, TC-10]
must_haves:
  truths:
    - "The file `.github/workflows/build-wheels-cuda-windows.yaml` exists at repo root and is valid YAML parseable by `actionlint`."
    - "The workflow is triggered ONLY by `workflow_dispatch` — no `push`, `pull_request`, `release`, or `schedule` keys are present in the `on:` block."
    - "Dispatching the workflow from the GitHub Actions UI presents exactly five inputs: `python_version` (choice, default `3.11`, options `['3.10','3.11','3.12']`), `cuda_version` (choice, default `12.4.1`, options `['12.4.1']`), `msvc_toolset` (string, default `14.39`), `verbose` (boolean, default false), `dry_run` (boolean, default false)."
    - "A `lint-workflow` job on `ubuntu-latest` runs FIRST (parallel, no `needs:`) and performs a ban-grep for the literal string `allow-unsupported-compiler` in the Windows workflow file; any match fails the job with exit 1."
    - "The `lint-workflow` job also runs `actionlint` as a second step (Claude's discretion per RESEARCH.md Open Question 4 — cheap, in-scope, already-paid Ubuntu runner); schema/expression errors fail the job."
    - "A `preflight` job stub exists with `runs-on: windows-2022` (explicit — never `windows-latest`), `defaults: run: shell: pwsh`, and `timeout-minutes: 20` — body to be filled by Plan 02."
    - "Running `actionlint .github/workflows/build-wheels-cuda-windows.yaml` locally exits 0; running `grep -F 'allow-unsupported-compiler' .github/workflows/build-wheels-cuda-windows.yaml` exits non-zero (zero matches)."
  artifacts:
    - path: ".github/workflows/build-wheels-cuda-windows.yaml"
      provides: "Two-job skeleton workflow file: trigger block + five dispatch inputs + permissions block + preflight job stub + lint-workflow job with ban-grep and actionlint wired"
      contains: "on: workflow_dispatch, python_version, cuda_version, msvc_toolset, verbose, dry_run, runs-on: windows-2022, runs-on: ubuntu-latest, allow-unsupported-compiler (inside the grep command literal only), actionlint"
      min_lines: 60
  key_links:
    - from: ".github/workflows/build-wheels-cuda-windows.yaml (lint-workflow job)"
      to: ".github/workflows/build-wheels-cuda-windows.yaml (self)"
      via: "bash grep -F scoped to THIS file only — not upstream's build-wheels-cuda.yaml, not CMakeLists.txt, not repo-wide"
      pattern: "grep -nH 'allow-unsupported-compiler'"
    - from: ".github/workflows/build-wheels-cuda-windows.yaml (on: workflow_dispatch)"
      to: "GitHub Actions UI dropdowns"
      via: "type: choice for python_version and cuda_version — renders as a dropdown that self-documents the valid set"
      pattern: "type: choice\\s+default:\\s+'3.11'"
---

<objective>
Create the Phase 1 workflow scaffold and wire the `lint-workflow` job FIRST so every subsequent commit to this file is parseable and ban-grep-clean before the more expensive preflight assertions land.

Purpose: Wave 0 establishes the substrate that every later task verifies against. Without this plan's deliverable, tasks in Plan 02 and Plan 03 have no file to edit, no local quick-command (`actionlint` + ban-grep) that returns meaningful signal, and no Ubuntu-side green/red check on the Actions dashboard.

Output: `.github/workflows/build-wheels-cuda-windows.yaml` with (a) top-of-file comment preamble placeholder (real DOC-04 text lands in Plan 03), (b) `on: workflow_dispatch` with five inputs, (c) `permissions: contents: read`, (d) `preflight` job stub (runs-on: windows-2022, defaults + timeout — body empty), (e) `lint-workflow` job with ban-grep + actionlint steps fully operational.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-scaffold-toolchain-pinning/01-CONTEXT.md
@.planning/phases/01-scaffold-toolchain-pinning/01-RESEARCH.md
@.planning/phases/01-scaffold-toolchain-pinning/01-VALIDATION.md

<!-- Reference file — DO NOT EDIT. Lifted only for pattern cross-check (trigger block, action pins). -->
@.github/workflows/build-wheels-cuda.yaml

<interfaces>
<!-- Key contracts Plan 02 and Plan 03 will consume. Executor should populate these shapes exactly. -->

Dispatch input contract (consumed by every step in Plan 02 via `${{ inputs.* }}`):
```yaml
inputs:
  python_version:
    description: 'Python version (CPython tag for the wheel)'
    type: choice
    default: '3.11'
    options: ['3.10', '3.11', '3.12']   # 3.13 excluded — MX-03/v2
  cuda_version:
    description: 'CUDA toolkit version (must match nvcc major.minor on runner)'
    type: choice
    default: '12.4.1'
    options: ['12.4.1']                 # single-item is intentional — MX-02/v2
  msvc_toolset:
    description: 'MSVC toolset MAJOR.MINOR (14.39 = VS 17.9, _MSC_VER 1939; raise only to work around runner image rotation — see upstream #1543)'
    type: string
    default: '14.39'
  verbose:
    description: 'Enable verbose preflight tracing (sets $env:VERBOSE=1)'
    type: boolean
    default: false
  dry_run:
    description: 'Exit green after preflight; skip downstream build (Phase 2+)'
    type: boolean
    default: false
```

Job layout contract (Plan 02 adds steps INSIDE the `preflight` job; no new jobs added until Phase 2):
```yaml
jobs:
  preflight:
    runs-on: windows-2022        # TC-01 — explicit, never windows-latest
    timeout-minutes: 20
    defaults:
      run:
        shell: pwsh              # every run block defaults to PowerShell
    steps: []                    # empty in Plan 01; populated in Plan 02 + Plan 03

  lint-workflow:
    runs-on: ubuntu-latest       # cheaper minutes, runs in parallel with preflight
    timeout-minutes: 5
    # no `needs:` — runs in parallel
    steps: [ban-grep, actionlint]  # fully populated in Plan 01
```

Ban-grep scope contract (DO NOT widen in future plans without roadmap update):
- Greps EXACTLY ONE file: `.github/workflows/build-wheels-cuda-windows.yaml`
- Does NOT grep: upstream's `build-wheels-cuda.yaml`, `CMakeLists.txt`, `pyproject.toml`, repo-wide
- Rationale: upstream's file legitimately contains the flag; we don't own it. CONTEXT.md § "Ban-grep self-check (TC-10)".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create workflow skeleton with trigger, inputs, preflight stub, and operational lint-workflow job</name>
  <files>.github/workflows/build-wheels-cuda-windows.yaml</files>
  <action>
Create a NEW file at `.github/workflows/build-wheels-cuda-windows.yaml` (do NOT edit the existing upstream file `.github/workflows/build-wheels-cuda.yaml`; they must coexist — WF-01).

Structure the file in this exact order:

1) **Top-of-file comment block** (placeholder — Plan 03 will replace with full DOC-04 preamble). Keep it minimal but include: one-line file purpose + "Phase 1 scaffold — see .planning/phases/01-scaffold-toolchain-pinning/". This is a placeholder because DOC-04 inline-comment coverage is Plan 03's requirement; this plan only needs the file to exist and parse.

2) **`name:` key** — set to `Build Wheels (CUDA Windows)`.

3) **`on: workflow_dispatch:` block** — WF-02 requires this be the ONLY trigger. Do NOT add `push`, `pull_request`, `release`, `schedule`, or `workflow_call`. Include all five inputs exactly as specified in the `<interfaces>` block above:
   - `python_version` — choice, default `'3.11'`, options `['3.10', '3.11', '3.12']` (WF-03).
   - `cuda_version` — choice, default `'12.4.1'`, options `['12.4.1']` (single-item on purpose — WF-04).
   - `msvc_toolset` — type `string`, default `'14.39'`.
   - `verbose` — boolean, default `false`.
   - `dry_run` — boolean, default `false`.

4) **`permissions:` block** at workflow level: `contents: read`. Principle of least privilege — Phase 1 only reads; Phase 4 publish job will widen via job-scoped override (RESEARCH.md Pitfall 7).

5) **`jobs:` block** with EXACTLY two jobs:

   a) **`preflight` job (STUB — body comes in Plan 02):**
      - `runs-on: windows-2022` (TC-01 — literal string, never `windows-latest`).
      - `timeout-minutes: 20`.
      - `defaults: run: shell: pwsh`.
      - `steps:` — include a single placeholder step that emits `"preflight stub — Plan 02 adds body"` via `Write-Host` so the YAML validates (a job with zero steps fails `actionlint`). This placeholder will be deleted by Plan 02's first task; mark it with a comment like `# PLACEHOLDER — delete when Plan 02 populates preflight body`.

   b) **`lint-workflow` job (FULLY OPERATIONAL in this plan):**
      - `runs-on: ubuntu-latest`.
      - `timeout-minutes: 5`.
      - No `needs:` — runs in parallel with `preflight`.
      - Steps in order:
        1. `actions/checkout@v4` (no `submodules:` needed — this job only reads the workflow file itself).
        2. **Ban-grep step** named `'Lint: ban --allow-unsupported-compiler'` — TC-10. Use bash `shell: bash` (default on Ubuntu). Implementation (from RESEARCH.md § "Ban-grep self-check"):
           ```
           set -euo pipefail
           WORKFLOW='.github/workflows/build-wheels-cuda-windows.yaml'
           if grep -nH 'allow-unsupported-compiler' "$WORKFLOW"; then
             echo "::error file=$WORKFLOW::'-allow-unsupported-compiler' is banned (upstream #1543)"
             exit 1
           fi
           echo "OK: no --allow-unsupported-compiler in $WORKFLOW"
           ```
           **Critical:** the grep target MUST be the literal path `.github/workflows/build-wheels-cuda-windows.yaml`. Do NOT grep `**/*.yaml`, `**/*.yml`, or repo-wide — upstream's `build-wheels-cuda.yaml` legitimately contains the flag (reference docstring) and must NOT be flagged.
        3. **actionlint step** named `'Lint: actionlint'` (Claude's discretion per RESEARCH.md Open Question 4; CONTEXT.md left the door open for inclusion in the `lint-workflow` job). Install via the official one-line script then run against the Windows workflow file:
           ```
           set -euo pipefail
           bash <(curl -sSL https://raw.githubusercontent.com/rhysd/actionlint/main/scripts/download-actionlint.bash)
           ./actionlint -color .github/workflows/build-wheels-cuda-windows.yaml
           ```
           Do NOT use a GitHub Action for actionlint — the one-line curl install is simpler and matches the "no external dependencies beyond what the job needs" ethos.

**Pin versions:** `actions/checkout@v4` only — no other actions needed in Plan 01. Version pins for `actions/setup-python`, `conda-incubator/setup-miniconda`, `ilammy/msvc-dev-cmd` are Plan 02's concern.

**What to AVOID in this task (surfaced by RESEARCH.md anti-patterns):**
- Do NOT add `permissions: contents: write` at workflow level (Pitfall 7).
- Do NOT add `submodules: recursive` to the `lint-workflow` checkout — that job doesn't need the submodule.
- Do NOT add a `needs:` clause between `lint-workflow` and `preflight` — parallelism is the whole point (RESEARCH.md § "Pattern 1: Two Parallel Jobs").
- Do NOT add a `matrix:` to either job — Phase 1 v1 has a single runner/Python/CUDA combo.
- Do NOT use `windows-latest` anywhere (TC-01).
- Do NOT put the ban-grep on Windows — bash + grep on Ubuntu is shorter and free (RESEARCH.md anti-pattern list).
- Do NOT add an `on: push:` trigger to get "CI on PR" coverage — WF-02 locks to `workflow_dispatch` only. Auto-triggers are v2 AUT-*.
- Do NOT widen the ban-grep scope to `**/*.yaml`. The phase boundary is THIS file only.

**Sanity check before committing:** run the verify command locally. If `actionlint` isn't installed on the dev machine, install it via `go install github.com/rhysd/actionlint/cmd/actionlint@latest` or `choco install actionlint`. If neither is available, the `lint-workflow` job itself will catch issues on the next dispatch — but local feedback is 2s vs 12min so it's worth the one-time setup.
  </action>
  <verify>
    <automated>actionlint .github/workflows/build-wheels-cuda-windows.yaml && grep -c -F "allow-unsupported-compiler" .github/workflows/build-wheels-cuda-windows.yaml | grep -q "^1$"</automated>
    <note>The grep count check expects exactly 1 occurrence: the literal inside the ban-grep command string itself (e.g., `grep -nH 'allow-unsupported-compiler' ...`). Zero occurrences means the ban-grep step is missing; 2+ occurrences means the banned flag has leaked somewhere else in the file.</note>
    <additional>yq '.on | keys | length' .github/workflows/build-wheels-cuda-windows.yaml returns 1 (only `workflow_dispatch`)</additional>
    <additional>yq '.on.workflow_dispatch.inputs | keys' .github/workflows/build-wheels-cuda-windows.yaml returns `[cuda_version, dry_run, msvc_toolset, python_version, verbose]` (order may vary — set membership is what matters)</additional>
    <additional>yq '.jobs | keys' .github/workflows/build-wheels-cuda-windows.yaml returns `[lint-workflow, preflight]`</additional>
    <additional>yq '.jobs.preflight."runs-on"' .github/workflows/build-wheels-cuda-windows.yaml returns `windows-2022` (not `windows-latest`)</additional>
    <additional>yq '.jobs."lint-workflow"."runs-on"' .github/workflows/build-wheels-cuda-windows.yaml returns `ubuntu-latest`</additional>
    <additional>yq '.jobs."lint-workflow".needs' .github/workflows/build-wheels-cuda-windows.yaml returns `null` (parallel, no dependency on preflight)</additional>
  </verify>
  <done>
File `.github/workflows/build-wheels-cuda-windows.yaml` exists, is valid YAML, parses cleanly under `actionlint`. The `lint-workflow` job contains a ban-grep step scoped to this file only (exactly one match of the literal string — inside the grep command itself) and an `actionlint` step. The `preflight` job exists as a stub with `runs-on: windows-2022`, `timeout-minutes: 20`, `defaults.run.shell: pwsh`, and a single placeholder step that Plan 02 will replace. All five dispatch inputs are present with the exact defaults and types specified.
  </done>
</task>

</tasks>

<verification>
Run the quick command locally (per 01-VALIDATION.md sampling rate "After every task commit"):
```
actionlint .github/workflows/build-wheels-cuda-windows.yaml
grep -nH 'allow-unsupported-compiler' .github/workflows/build-wheels-cuda-windows.yaml
```
Expected: actionlint exits 0; grep returns exactly ONE line (the ban-grep step's own command literal — the only legitimate occurrence in our file). If grep returns zero lines, the ban-grep step is missing; if it returns 2+ lines, the banned flag has leaked somewhere real and must be purged.

No dispatch required in this wave — the workflow has no toolchain body yet, so dispatching would only exercise the `lint-workflow` job which is already covered by the local actionlint + grep checks.
</verification>

<success_criteria>
- `.github/workflows/build-wheels-cuda-windows.yaml` exists at repo root.
- `actionlint` on that file exits 0 (valid YAML, all `${{ inputs.* }}` expressions resolve, no deprecated syntax).
- `grep -F 'allow-unsupported-compiler'` returns exactly one line — the ban-grep command in the lint job itself (proof the step is in place and no banned usage has leaked).
- `yq '.on | keys'` returns exactly `['workflow_dispatch']` (no `push` / `pull_request` / `release` / `schedule` / `workflow_call`).
- All five dispatch inputs present with the exact defaults specified in the contract.
- `preflight` job runs on `windows-2022` literal with 20-min timeout and pwsh default shell.
- `lint-workflow` job runs on `ubuntu-latest` with no `needs:` dependency.
- Upstream `build-wheels-cuda.yaml` is UNTOUCHED (verify with `git diff .github/workflows/build-wheels-cuda.yaml` — no output).
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-toolchain-pinning/01-01-SUMMARY.md` capturing:
- The exact PowerShell/bash idioms chosen (so Plan 02 can reuse the same strict-mode prelude).
- The `actionlint` install method chosen (curl-one-liner vs GitHub Action) so Plan 03 doesn't re-decide.
- Any deviations from the interfaces block (there should be none — flag loudly if forced).
- Confirmation that `grep -nH 'allow-unsupported-compiler' .github/workflows/build-wheels-cuda-windows.yaml` returns exactly the ban-grep command literal and nothing else.
</output>
