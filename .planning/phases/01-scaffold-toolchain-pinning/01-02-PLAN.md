---
phase: 01-scaffold-toolchain-pinning
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .github/workflows/build-wheels-cuda-windows.yaml
autonomous: false
requirements: [WF-05, TC-02, TC-03, TC-04, TC-05, TC-06, TC-07, TC-08, TC-09]
must_haves:
  truths:
    - "A dispatcher running the workflow with default inputs (`python_version=3.11`, `cuda_version=12.4.1`, `msvc_toolset=14.39`) reaches the end of the `preflight` job with green status on `windows-2022`."
    - "The job log shows `cl.exe /Bv` reporting a `Version 19.39.*` banner (equivalently `_MSC_VER = 1939`); the MSC_VER cap assertion passes because 1939 ≤ 1939 (cap derived from `14.39` → `1900 + 39`)."
    - "The job log shows `nvcc --version` containing `release 12.4`, matching the major.minor of the dispatched `cuda_version` (12.4.1 → 12.4)."
    - "`where.exe nvcc` returns exactly one path, and that path starts with `$env:CONDA_PREFIX` (the mamba-managed CUDA, no ghost Jimver install)."
    - "`Get-ChildItem env: | Where Name -match '^CUDA_PATH_V\\d+_\\d+$'` returns zero entries after the sweep step runs."
    - "`vendor/llama.cpp/CMakeLists.txt` exists after `actions/checkout@v4` with `submodules: recursive`; `git -C vendor/llama.cpp rev-parse --short HEAD` returns a non-empty 7-char SHA."
    - "`python --version` reports the dispatched `python_version` major.minor (e.g., `Python 3.11.*` for `python_version=3.11`)."
    - "VS BuildCustomizations path is discovered dynamically via `vswhere -latest -property installationPath` + glob; strict-mode fails the step if no matching path is found (no hardcoded `2019\\Enterprise` anywhere)."
    - "Every `run:` block using `shell: pwsh` starts with `Set-StrictMode -Version Latest` and `$ErrorActionPreference = 'Stop'` on the first two non-blank lines."
    - "`HKLM:\\SYSTEM\\CurrentControlSet\\Control\\FileSystem\\LongPathsEnabled` reads as `1` after the enable-long-paths step (verified in the same step via `Get-ItemProperty`)."
    - "Each named preflight assertion step is its OWN step (one step per assertion, named descriptively) — no monolithic 'run all 5 checks' step — so the Actions UI pinpoints which assertion failed."
    - "A dispatch with `msvc_toolset=14.01` (negative test — below-floor bogus value) hard-fails at the MSVC probe step with a named-step error and a remediation hint; does NOT silently continue and does NOT reach the `nvcc` assertion."
  artifacts:
    - path: ".github/workflows/build-wheels-cuda-windows.yaml"
      provides: "Populated `preflight` job body: LongPathsEnabled + checkout (with submodules) + setup-python + mamba/CUDA install + MSVC 14.39 probe+install + ilammy/msvc-dev-cmd activation + 5 named preflight assertion steps + dynamic VS BuildCustomizations discovery"
      contains: "Set-StrictMode -Version Latest, vswhere, vs_installer, ilammy/msvc-dev-cmd@v1, conda-incubator/setup-miniconda@v3.1.0, Microsoft.VisualStudio.Component.VC, submodules: recursive, LongPathsEnabled, cl.exe, nvcc --version, CUDA_PATH_V, vendor/llama.cpp/CMakeLists.txt"
      min_lines: 180
  key_links:
    - from: "Preflight assertion 'Assert MSVC _MSC_VER <= cap'"
      to: "${{ inputs.msvc_toolset }}"
      via: "$parts = $toolset -split '\\.'; $cap = 1900 + [int]$parts[1] — cap is COMPUTED from the dispatch input, not hardcoded 1939"
      pattern: "1900 \\+ \\[?int\\]?\\s*\\$?parts\\[1\\]|1900 \\+ \\$minor"
    - from: "Preflight assertion 'Assert nvcc matches cuda_version'"
      to: "${{ inputs.cuda_version }}"
      via: "regex match on `release (\\d+\\.\\d+)` from nvcc output; compare major.minor to first two dotted components of cuda_version input"
      pattern: "release \\(\\\\d\\+\\\\.\\\\d\\+\\)"
    - from: "MSVC 14.39 install step"
      to: "Activation step (ilammy/msvc-dev-cmd@v1)"
      via: "probe-first-install-second pattern: vswhere checks component availability BEFORE invoking vs_installer.exe modify; post-install re-probe confirms component is on disk BEFORE ilammy activates it"
      pattern: "vswhere.*-requires.*Microsoft\\.VisualStudio\\.Component\\.VC"
    - from: "actions/checkout@v4 (with submodules: recursive)"
      to: "Assertion 4 (submodule populated)"
      via: "Test-Path vendor/llama.cpp/CMakeLists.txt — catches silent submodule checkout failures (RESEARCH.md Pitfall 2)"
      pattern: "Test-Path\\s+['\"]?vendor/llama\\.cpp/CMakeLists\\.txt"
---

<objective>
Populate the `preflight` job body with the toolchain install sequence (LongPaths → checkout → setup-python → mamba CUDA → MSVC 14.39 probe+install+activate) followed by the five named preflight assertion steps plus dynamic VS BuildCustomizations discovery.

Purpose: This plan delivers the core phase value — a preflight that fails LOUDLY on any toolchain drift rather than silently producing wheels that segfault at runtime (upstream #1543). Every assertion is a named step so the Actions UI pinpoints which check failed; every `pwsh` block uses strict-mode so `$null`-propagation bugs (the root cause of upstream's MSBuildExtensions silent no-op) turn into hard errors.

Output: Expanded `.github/workflows/build-wheels-cuda-windows.yaml` with the `preflight` job body fully populated. `lint-workflow` job is UNCHANGED from Plan 01. Forensics `$GITHUB_STEP_SUMMARY` step and top-of-file DOC-04 comment block are Plan 03's concern.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold-toolchain-pinning/01-CONTEXT.md
@.planning/phases/01-scaffold-toolchain-pinning/01-RESEARCH.md
@.planning/phases/01-scaffold-toolchain-pinning/01-VALIDATION.md
@.planning/phases/01-scaffold-toolchain-pinning/01-01-SUMMARY.md
@.github/workflows/build-wheels-cuda-windows.yaml

<!-- Reference only — do NOT edit. Upstream's Windows steps are ~80% relevant patterns. -->
@.github/workflows/build-wheels-cuda.yaml

<interfaces>
<!-- Existing contracts from Plan 01 (do not change): -->
<!-- - `on.workflow_dispatch.inputs` shape (5 inputs, locked). -->
<!-- - `preflight` job stub with `runs-on: windows-2022`, `timeout-minutes: 20`, `defaults.run.shell: pwsh`. -->
<!-- - Placeholder step in `preflight.steps` — DELETE this step as the first edit of Task 1. -->

Strict-mode prelude (MUST appear at the top of EVERY `shell: pwsh` run block — non-negotiable per RESEARCH.md Pattern 4):
```powershell
Set-StrictMode -Version Latest
$ErrorActionPreference = 'Stop'
```

Step-ID contract (Plan 03's forensics summary references these IDs via `${{ steps.<id>.outputs.* }}` — names MUST match):
- Step id `discover-vs` → outputs `vs_install_path`, `bc_path`
- Step id `assert-msvc` → outputs `msc_ver`
- Step id `assert-nvcc` → outputs `nvcc_version`
- Step id `assert-submodule` → outputs `llama_sha`
- Step id `assert-python` → outputs `python_version`, `python_path`

Component-ID derivation (MSVC install step):
```powershell
# toolset='14.39' → componentId='Microsoft.VisualStudio.Component.VC.14.39.17.9.x86.x64'
$componentId = "Microsoft.VisualStudio.Component.VC.$toolset.17.9.x86.x64"
```
NOTE: The `.17.9.` suffix is the VS version that originally shipped the toolset (14.39 ships with VS 17.9). If `msvc_toolset` is bumped to `14.40`, the suffix would become `.17.10.` — but that is MX-02/v2 territory; for Phase 1 the suffix is hardcoded `17.9` with a comment noting the coupling.

_MSC_VER cap formula (hard-locked per RESEARCH.md Pattern 3):
```powershell
$parts = $toolset -split '\.'
if ($parts.Count -ne 2) { throw "msvc_toolset must be 'MAJOR.MINOR'; got '$toolset'" }
$major = [int]$parts[0]; $minor = [int]$parts[1]
if ($major -ne 14) { throw "msvc_toolset major must be 14 for v1 (got '$major')" }
$cap = 1900 + $minor   # 14.39 → 1939; 14.40 → 1940
```

VS 17.x pin (passed to `ilammy/msvc-dev-cmd@v1`):
```yaml
vs-version: '[17.9,17.10)'   # tight pin per DOC-04; VS 17.10+ trips preflight
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add toolchain install chain (LongPaths → checkout → setup-python → mamba/CUDA → MSVC 14.39 probe+install+activate)</name>
  <files>.github/workflows/build-wheels-cuda-windows.yaml</files>
  <behavior>
Post-task, the `preflight` job's `steps:` list (in this order, with named steps) must be:
1. `Enable Windows long paths` (TC-09)
2. `actions/checkout@v4` with `submodules: recursive` (WF-05)
3. `actions/setup-python@v5` with `python-version: ${{ inputs.python_version }}` and `cache: 'pip'`
4. `Probe and install MSVC ${{ inputs.msvc_toolset }} toolset` (TC-02) — probe-first-install-second pattern
5. `Activate MSVC ${{ inputs.msvc_toolset }} toolset` — uses `ilammy/msvc-dev-cmd@v1` (TC-03)
6. `Setup mamba` — uses `conda-incubator/setup-miniconda@v3.1.0`
7. `Install CUDA toolkit via mamba` (TC-05)

The Plan 01 placeholder step is DELETED. No assertions yet — those come in Task 2.
  </behavior>
  <action>
Open `.github/workflows/build-wheels-cuda-windows.yaml` from Plan 01. Under the `preflight` job's `steps:`:

1. **DELETE the placeholder step** that Plan 01 added. It should be the only existing step in `preflight`.

2. **Add Step 1 — Enable Windows long paths (TC-09).** Named `'Enable Windows long paths'`. Uses `shell: pwsh` (redundant with job default but explicit for clarity). Body (from RESEARCH.md § "LongPathsEnabled"):
   ```powershell
   Set-StrictMode -Version Latest
   $ErrorActionPreference = 'Stop'
   New-ItemProperty `
     -Path 'HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem' `
     -Name 'LongPathsEnabled' `
     -Value 1 `
     -PropertyType DWORD `
     -Force | Out-Null
   git config --system core.longpaths true
   $enabled = (Get-ItemProperty 'HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem').LongPathsEnabled
   if ($enabled -ne 1) { throw "LongPathsEnabled failed to persist (got $enabled)" }
   Write-Host "LongPathsEnabled: $enabled"
   ```
   **Critical:** this step MUST come BEFORE `actions/checkout@v4`. Checkout will clone `vendor/llama.cpp/ggml/src/ggml-cuda/...` paths that exceed 260 chars; without this key set first, the clone silently truncates on some runner warm-start scenarios.

3. **Add Step 2 — Checkout (WF-05).** `uses: actions/checkout@v4`. Inputs:
   ```yaml
   with:
     submodules: recursive
     fetch-depth: 1
   ```
   `fetch-depth: 1` is fine for Phase 1 — we don't need history. Phase 2 (version-embedding with submodule SHA) will confirm `git -C vendor/llama.cpp rev-parse --short HEAD` works with `fetch-depth: 1` (it does — submodule has its own clone).

4. **Add Step 3 — Setup Python.** `uses: actions/setup-python@v5`. Inputs:
   ```yaml
   with:
     python-version: ${{ inputs.python_version }}
     cache: 'pip'
   ```
   This provides the host Python referenced by Assertion 5. Mamba will install its own Python inside the activated env — both must agree in major.minor; Assertion 5 catches mismatches.

5. **Add Step 4 — MSVC 14.39 probe+install (TC-02).** Named `'Probe and install MSVC ${{ inputs.msvc_toolset }} toolset'`. Full probe-first-install-second pattern per RESEARCH.md § "MSVC 14.39 probe + install":
   - Build `$componentId` using the derivation in `<interfaces>` (`Microsoft.VisualStudio.Component.VC.$toolset.17.9.x86.x64`).
   - Locate `vswhere.exe` at `C:\Program Files (x86)\Microsoft Visual Studio\Installer\vswhere.exe`.
   - Probe: `& $vswhere -latest -property installationPath` — if empty, `throw` with "No VS 2022 installation found".
   - Probe: `& $vswhere -products * -requires $componentId -property installationPath` — if empty, `Write-Error` with the full remediation hint block from RESEARCH.md (references `actions/runner-images#9701` and upstream `abetlen/llama-cpp-python#1543`, suggests `msvc_toolset=14.40` as fallback), then `exit 1`.
   - Install: locate `setup.exe` relative to `$installPath` (`Join-Path $installPath '..\..\..\..\Installer\setup.exe' | Resolve-Path`) and invoke `Start-Process -Wait -PassThru -FilePath $installer -ArgumentList @('modify', '--installPath', "`"$installPath`"", '--add', $componentId, '--quiet', '--norestart')`. Accept exit codes 0 AND 3010 (3010 = success-with-reboot-required, acceptable on CI); any other code throws.
   - Post-install re-probe: same `vswhere ... -requires $componentId` query. If still empty, `throw` — the installer silently failed.
   - Add `timeout-minutes: 15` on the step (install can legitimately take 10 min; 15 is the bound).

6. **Add Step 5 — Activate MSVC (TC-03).** Named `'Activate MSVC ${{ inputs.msvc_toolset }} toolset'`. Full block:
   ```yaml
   uses: ilammy/msvc-dev-cmd@v1
   with:
     toolset: ${{ inputs.msvc_toolset }}
     vs-version: '[17.9,17.10)'
     arch: x64
   ```
   **Do NOT** substitute `microsoft/setup-msbuild@v2` — it selects among installed VS versions but cannot narrow to a specific toolset (see RESEARCH.md § "State of the Art" table and § "Alternatives Considered"). `ilammy/msvc-dev-cmd` wraps `vcvarsall.bat -vcvars_ver=...` which is the actual working mechanism.

7. **Add Step 6 — Setup mamba.** `uses: conda-incubator/setup-miniconda@v3.1.0` (exact pin, matches upstream for determinism). Inputs:
   ```yaml
   with:
     activate-environment: llamacpp
     python-version: ${{ inputs.python_version }}
     miniforge-version: latest
     add-pip-as-python-dependency: true
     auto-activate-base: false
   ```

8. **Add Step 7 — Install CUDA via mamba (TC-05).** Named `'Install CUDA toolkit via mamba'`. Use `shell: bash -el {0}` (per RESEARCH.md — `-l` triggers conda init, `-e` fail-fast). Body (from RESEARCH.md § "CUDA install"):
   ```bash
   set -euo pipefail
   CUDA_VERSION='${{ inputs.cuda_version }}'
   CUDA_CHANNEL="nvidia/label/cuda-${CUDA_VERSION}"
   # TC-05: single install path. No Jimver full-installer.
   # --channel-priority strict prevents conda-forge from pulling a mismatched cudart.
   mamba install -y \
     --channel-priority strict \
     --override-channels \
     -c "${CUDA_CHANNEL}" \
     "${CUDA_CHANNEL}::cuda-toolkit=${CUDA_VERSION}"
   ```
   Add `timeout-minutes: 15` on the step.

**What to AVOID in this task:**
- Do NOT add `Jimver/cuda-toolkit@*` anywhere (TC-05 — single install path). Upstream's Jimver usage is for MSBuildExtensions extraction, which is Phase 2's problem (if needed at all).
- Do NOT hardcode `_MSC_VER` caps or VS edition paths; the cap derivation is Task 2 territory but component-ID construction here must use `${{ inputs.msvc_toolset }}` — never hardcode `14.39` in string literals.
- Do NOT add step-level `env:` blocks setting `CUDA_PATH=...` — mamba's activation handles it. Manual `CUDA_PATH` writes are exactly what creates the `CUDA_PATH_V*_*` ghost vars (Pitfall 4).
- Do NOT wrap any step in a `continue-on-error: true` — the whole phase's thesis is "hard-fail, never silent drift."
- Do NOT change anything in the `lint-workflow` job.
- Do NOT add `-DCMAKE_CUDA_FLAGS=--allow-unsupported-compiler` anywhere (TC-10, enforced by the `lint-workflow` ban-grep — this bullet is a reminder that the ban applies to new content too).
  </action>
  <verify>
    <automated>actionlint .github/workflows/build-wheels-cuda-windows.yaml && grep -c -F "allow-unsupported-compiler" .github/workflows/build-wheels-cuda-windows.yaml | grep -q "^1$"</automated>
    <note>Same quick command as Plan 01 — local feedback in 2s per 01-VALIDATION.md sampling rate "After every task commit".</note>
    <additional>yq '.jobs.preflight.steps | length' .github/workflows/build-wheels-cuda-windows.yaml returns 7</additional>
    <additional>grep -c -F 'ilammy/msvc-dev-cmd@v1' .github/workflows/build-wheels-cuda-windows.yaml returns 1</additional>
    <additional>grep -c -F 'conda-incubator/setup-miniconda@v3.1.0' .github/workflows/build-wheels-cuda-windows.yaml returns 1</additional>
    <additional>grep -c -F 'submodules: recursive' .github/workflows/build-wheels-cuda-windows.yaml returns 1</additional>
    <additional>grep -c -E 'Jimver/cuda-toolkit' .github/workflows/build-wheels-cuda-windows.yaml returns 0 (TC-05 single-install-path enforcement)</additional>
    <additional>grep -c -F 'LongPathsEnabled' .github/workflows/build-wheels-cuda-windows.yaml returns at least 2 (New-ItemProperty Name + post-write Get-ItemProperty verify)</additional>
  </verify>
  <done>
The `preflight` job now has exactly 7 named steps in order: LongPaths → Checkout → Setup-Python → MSVC probe+install → MSVC activate → Setup mamba → CUDA install. Every `pwsh` run block begins with the strict-mode prelude. No Jimver action is present. The Plan 01 placeholder step is deleted. `actionlint` exits 0; ban-grep exactly one (unchanged from Plan 01 baseline).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add 5 preflight assertions + dynamic VS BuildCustomizations discovery (one named step each)</name>
  <files>.github/workflows/build-wheels-cuda-windows.yaml</files>
  <behavior>
Post-task, the `preflight` job gains six MORE named steps AFTER the seven from Task 1:
8. `id: assert-msvc` — `'Preflight: Assert MSVC _MSC_VER <= cap'` (TC-04)
9. `id: assert-nvcc` — `'Preflight: Assert nvcc matches cuda_version'` (TC-06)
10. `id: assert-cuda-single-path` — `'Preflight: Assert single CUDA install path'` (TC-07)
11. `id: assert-submodule` — `'Preflight: Assert llama.cpp submodule populated'` (WF-05 double-check)
12. `id: assert-python` — `'Preflight: Assert Python version'` (Assertion 5 from CONTEXT.md)
13. `id: discover-vs` — `'Preflight: Discover VS 2022 BuildCustomizations path'` (TC-08)

Negative-case behavior (manual test per 01-VALIDATION.md): dispatching with `msvc_toolset=14.01` hard-fails at Step 4 (probe) OR at Step 8 (assert-msvc) depending on whether vswhere even recognizes the bogus component — either way, the job MUST go red with a named-step error, NEVER silently continue.
  </behavior>
  <action>
Open `.github/workflows/build-wheels-cuda-windows.yaml` from Task 1. Append six new steps to the `preflight` job's `steps:` after the CUDA install step. Each assertion is its OWN step with its own name (monolithic "run all 5 checks" is explicitly banned by CONTEXT.md § "Preflight scope & structure").

**Universal rule:** every step uses `shell: pwsh` (redundant with job default — explicit) and begins with:
```powershell
Set-StrictMode -Version Latest
$ErrorActionPreference = 'Stop'
```

Every assertion, on failure, must emit a multi-line `Write-Error` (or `throw`) block containing a remediation hint (not just the raw assertion output). Use PowerShell here-strings (`@" ... "@`) for multi-line messages — RESEARCH.md § "Don't Hand-Roll" covers this.

**Step 8 — `id: assert-msvc` (TC-04).** Name: `'Preflight: Assert MSVC _MSC_VER <= cap'`. Pattern from RESEARCH.md § "Assertion 1 — MSVC toolset":
- Derive `$cap = 1900 + [int](($toolset -split '\.')[1])` (the whole point — cap MUST be computed from `${{ inputs.msvc_toolset }}`, not hardcoded `1939`).
- Validate `$parts.Count -eq 2`, `$major -eq 14` — throw on malformed input.
- Run `cl.exe 2>&1 | Out-String`, regex match `'Version (\d+)\.(\d+)'`, compute `$mscVer = $clMajor * 100 + $clMinor`.
- If `$mscVer -gt $cap`, `Write-Error` with remediation hint (references upstream #1543 and the probe-step log), then `exit 1`.
- On green: `Write-Host "OK: _MSC_VER=$mscVer ≤ cap=$cap"` AND emit `"msc_ver=$mscVer" >> $env:GITHUB_OUTPUT` so Plan 03's forensics summary can reference `${{ steps.assert-msvc.outputs.msc_ver }}`.

**Step 9 — `id: assert-nvcc` (TC-06).** Name: `'Preflight: Assert nvcc matches cuda_version'`. Pattern from RESEARCH.md § "Assertion 2 — CUDA toolkit":
- Extract major.minor from `${{ inputs.cuda_version }}` (e.g., `12.4.1 → 12.4`).
- Run `nvcc --version`, regex match `'release (\d+\.\d+)'`.
- Throw with remediation hint if mismatch (major.minor comparison — `nvcc` typically reports `12.4` not `12.4.1`).
- On green: emit `"nvcc_version=$nvccMajorMinor" >> $env:GITHUB_OUTPUT`.

**Step 10 — `id: assert-cuda-single-path` (TC-07).** Name: `'Preflight: Assert single CUDA install path'`. Pattern from RESEARCH.md § "Assertion 3":
- Sweep `Get-ChildItem env: | Where-Object { $_.Name -match '^CUDA_PATH_V\d+_\d+$' }`; log and `Remove-Item` each.
- Run `(where.exe nvcc 2>$null) -split "`n" | Where-Object { $_ -ne '' }`; assert count is exactly 1.
- Assert `$nvccPaths[0].StartsWith($env:CONDA_PREFIX)` (the one nvcc must be the mamba-managed one).
- Throw with remediation hint on any failure.
- No GITHUB_OUTPUT needed (Plan 03 doesn't reference this step's outputs — `$env:CUDA_PATH` is read directly in the forensics summary).

**Step 11 — `id: assert-submodule` (WF-05 double-check; RESEARCH.md Pitfall 2).** Name: `'Preflight: Assert llama.cpp submodule populated'`. Pattern from RESEARCH.md § "Assertion 4":
- `if (-not (Test-Path 'vendor/llama.cpp/CMakeLists.txt')) { throw ... }` with remediation hint (re-dispatch advice, `.gitmodules` check).
- `$llamaSha = (git -C vendor/llama.cpp rev-parse --short HEAD).Trim()`.
- Emit `"llama_sha=$llamaSha" >> $env:GITHUB_OUTPUT`.

**Step 12 — `id: assert-python` (Assertion 5 from CONTEXT.md).** Name: `'Preflight: Assert Python version'`. Pattern from RESEARCH.md § "Assertion 5":
- Parse `python --version 2>&1` with regex `'Python (\d+\.\d+)'`.
- Compare to `${{ inputs.python_version }}`; throw on mismatch with remediation hint (covers the `setup-python` vs `setup-miniconda` PATH-order trap from RESEARCH.md Pitfall 3).
- Emit `"python_version=$actual" >> $env:GITHUB_OUTPUT` and `"python_path=$pyPath" >> $env:GITHUB_OUTPUT`.

**Step 13 — `id: discover-vs` (TC-08).** Name: `'Preflight: Discover VS 2022 BuildCustomizations path'`. Pattern from RESEARCH.md § "Dynamic VS BuildCustomizations discovery":
- `$vswhere = 'C:\Program Files (x86)\Microsoft Visual Studio\Installer\vswhere.exe'`.
- `$vsRoot = & $vswhere -latest -property installationPath` — throw if empty.
- `$bcPattern = Join-Path $vsRoot 'MSBuild\Microsoft\VC\*\BuildCustomizations'`.
- `$bcPaths = Get-ChildItem -Path $bcPattern -ErrorAction SilentlyContinue`.
- If `-not $bcPaths -or $bcPaths.Count -eq 0`, throw with remediation hint — strict-mode is what flips this from silent no-op (upstream #1543 Pitfall 5) to loud error.
- Emit `"vs_install_path=$vsRoot" >> $env:GITHUB_OUTPUT` and `"bc_path=$($bcPaths[0].FullName)" >> $env:GITHUB_OUTPUT`.
- **Positioning:** This step comes AFTER `assert-python` — it's independent of mamba/nvcc and only needs VS to be installed, which it always is on `windows-2022`. Phase 2 will actually USE `bc_path` during the build; Phase 1 just verifies it's discoverable.

**Step ordering rationale:**
- MSVC / nvcc / CUDA-path checks cluster first (they test the install steps that Task 1 added).
- Submodule + Python come next (they test orthogonal concerns — checkout + setup-python respectively).
- VS BuildCustomizations comes last (no runtime dependency on earlier assertions; independent check).

**What to AVOID in this task:**
- Do NOT merge two assertions into one step to save lines (CONTEXT.md: one step per assertion, non-negotiable).
- Do NOT hardcode `1939` anywhere. Every reference to the MSC_VER cap must derive from `${{ inputs.msvc_toolset }}`. Grep your own work: `grep -F '1939' .github/workflows/build-wheels-cuda-windows.yaml` should return zero matches (the comment "1939 = 14.39" is allowed but ideally avoided).
- Do NOT use `warn-and-continue` / `continue-on-error: true` on any assertion. Every failure path is `throw` or `Write-Error + exit 1`. Warn-and-continue is the upstream #1543 failure mode.
- Do NOT skip the strict-mode prelude on any step. The `lint-workflow` job does NOT currently grep-enforce this; Plan 03 may add that grep-enforcement, but even without it, omission is a hard fail in code review.
- Do NOT emit `$GITHUB_STEP_SUMMARY` here — that's Plan 03's concern (forensics summary step). Outputs via `>> $env:GITHUB_OUTPUT` ARE required (Plan 03 consumes them).
- Do NOT add `actions/checkout` or any other `uses:` action in this task — all six steps are inline PowerShell.
  </action>
  <verify>
    <automated>actionlint .github/workflows/build-wheels-cuda-windows.yaml && grep -c -F "allow-unsupported-compiler" .github/workflows/build-wheels-cuda-windows.yaml | grep -q "^1$"</automated>
    <note>Local quick command (2s). Dispatch-based full validation happens AFTER this task via the checkpoint below.</note>
    <additional>yq '.jobs.preflight.steps | length' .github/workflows/build-wheels-cuda-windows.yaml returns 13 (7 from Task 1 + 6 from Task 2)</additional>
    <additional>grep -c -E "id:\s+(assert-msvc|assert-nvcc|assert-cuda-single-path|assert-submodule|assert-python|discover-vs)" .github/workflows/build-wheels-cuda-windows.yaml returns 6</additional>
    <additional>grep -c -F "Set-StrictMode -Version Latest" .github/workflows/build-wheels-cuda-windows.yaml returns at least 7 (one per new pwsh step + LongPaths step from Task 1; probe-install step from Task 1 also has it)</additional>
    <additional>grep -c -F '1900 + ' .github/workflows/build-wheels-cuda-windows.yaml returns at least 1 (the MSC_VER cap derivation is present and not hardcoded)</additional>
    <additional>grep -c -E "^\s*1939\s*$|['\"]1939['\"]" .github/workflows/build-wheels-cuda-windows.yaml returns 0 (no bare-literal 1939 — cap must be computed)</additional>
    <additional>grep -c -F "vendor/llama.cpp/CMakeLists.txt" .github/workflows/build-wheels-cuda-windows.yaml returns at least 1</additional>
    <additional>grep -c -E 'continue-on-error:\s*true' .github/workflows/build-wheels-cuda-windows.yaml returns 0 (no warn-and-continue paths)</additional>
  </verify>
  <done>
`preflight` job contains 13 named steps in order. Every assertion step has a unique `id:` matching the contract in the `<interfaces>` block of this plan (assert-msvc, assert-nvcc, assert-cuda-single-path, assert-submodule, assert-python, discover-vs). Every pwsh step begins with strict-mode + ErrorActionPreference. The MSC_VER cap is derived from `${{ inputs.msvc_toolset }}` (not hardcoded). No bare-literal `1939` appears as a numeric constant. `actionlint` exits 0; ban-grep unchanged.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Dispatch workflow end-to-end — validate happy-path green and negative-case hard-fail</name>
  <files>.github/workflows/build-wheels-cuda-windows.yaml (no edits — verification only)</files>
  <action>
Human verification step. Tasks 1 and 2 have built the complete `preflight` job body (13 steps). This gate confirms the toolchain behaves correctly under real dispatch — both happy-path and negative-case.

### What was built

Complete `preflight` job body: toolchain install chain (Task 1, 7 steps) + 5 named preflight assertions + 1 dynamic VS discovery step (Task 2, 6 steps) = 13 steps total. Plus the `lint-workflow` job from Plan 01 (unchanged).

This checkpoint exists because per 01-VALIDATION.md:
- "After every plan wave: Run quick command. (Full dispatch reserved for wave completion only — it costs ~10 runner minutes.)"
- "Before /gsd:verify-work: Full suite must be green — one successful workflow_dispatch run reaching the preflight job's terminal ... step with green status on BOTH lint-workflow and preflight jobs."

The DEFAULT-input dispatch validates the HAPPY PATH. The NEGATIVE-input dispatch validates that hard-fail behavior is real (not silent pass). Both are manual because they cost runner minutes and require UI interaction.

### Verification 1 — Default-input dispatch (HAPPY PATH)

1. Push the branch with Plan 01 + Plan 02 changes.
2. Go to the GitHub Actions UI for this repo's `build-wheels-cuda-windows.yaml` workflow.
3. Click `Run workflow` with DEFAULT inputs: `python_version=3.11`, `cuda_version=12.4.1`, `msvc_toolset=14.39`, `verbose=false`, `dry_run=false`.
4. Wait for the run to complete (~8-12 min).
5. **Expected outcome — green on BOTH jobs:**
   - `lint-workflow` (Ubuntu): completes in under 30s, green. Contains `actionlint` step output showing zero errors AND ban-grep step output `"OK: no --allow-unsupported-compiler ..."`.
   - `preflight` (Windows 2022): completes in ~8-12 min, green. ALL 13 named steps green. Log of `assert-msvc` shows `"OK: _MSC_VER=1939 <= cap=1939"`. Log of `assert-nvcc` shows `"OK: nvcc reports 12.4 matches requested 12.4"`. Log of `discover-vs` shows a non-null `vs_install_path`.

### Verification 2 — Negative-case dispatch (HARD-FAIL PROOF)

1. Dispatch AGAIN with `msvc_toolset=14.01` (intentionally-bogus value; 14.01 is below any shipped VS component).
2. **Expected outcome — RED at the MSVC probe step:**
   - `preflight` fails at Step 4 (`Probe and install MSVC 14.01 toolset`) with a named-step error. The log must contain the remediation hint block (references `actions/runner-images#9701` and `abetlen/llama-cpp-python#1543`, suggests `msvc_toolset=14.40`).
   - The job must NOT reach Step 8 (`assert-msvc`). If it does, the probe step is silently passing on bogus input — a critical bug.
   - `lint-workflow` must still be green (it's independent of the preflight input).

### Verification 3 — Upstream file untouched

Run `git diff main -- .github/workflows/build-wheels-cuda.yaml` locally and confirm empty output.

### Resume signal

Type "approved" if all three verifications pass. If green-path dispatch fails at any assertion step, paste the step name + log excerpt so we can diagnose. If the 14.01 negative case silently passes (job goes green), that is a CRITICAL regression and must be diagnosed before proceeding to Plan 03.
  </action>
  <verify>
    <automated>gh run list --workflow=build-wheels-cuda-windows.yaml --limit 2 --json conclusion --jq '.[0].conclusion == "failure" and .[1].conclusion == "success"'</automated>
    <note>Presumes the two most recent runs are (most-recent) negative-case red and (second-most-recent) happy-path green. Operator MUST confirm run ordering matches before accepting the automated signal — if CI history is polluted, use `gh run view RUN_ID` per dispatch instead.</note>
  </verify>
  <done>
Both dispatches have been run through the Actions UI. Default-input dispatch completed green on both `preflight` and `lint-workflow`. `msvc_toolset=14.01` dispatch completed red at the MSVC probe step with a remediation hint visible in the log. Upstream `build-wheels-cuda.yaml` has zero diff from main. Human has typed "approved" confirming all three verifications observed.
  </done>
</task>

</tasks>

<verification>
Quick command (local, runs after every task commit per 01-VALIDATION.md):
```
actionlint .github/workflows/build-wheels-cuda-windows.yaml
grep -nH 'allow-unsupported-compiler' .github/workflows/build-wheels-cuda-windows.yaml
```
Expected: actionlint exits 0; grep returns exactly one line (the ban-grep command literal in the lint-workflow job).

Full command (wave completion, per 01-VALIDATION.md — runs during the checkpoint above):
```
gh workflow run build-wheels-cuda-windows.yaml -f python_version=3.11 -f cuda_version=12.4.1 -f msvc_toolset=14.39
gh run watch --exit-status
```
Expected: single green run reaching `preflight` terminal step AND `lint-workflow` green.

Negative-case full command:
```
gh workflow run build-wheels-cuda-windows.yaml -f python_version=3.11 -f cuda_version=12.4.1 -f msvc_toolset=14.01
gh run watch  # expect non-zero exit from --exit-status
```
Expected: red at MSVC probe step with remediation hint; job does NOT reach assert-msvc.
</verification>

<success_criteria>
- 13 named steps in `preflight` job (7 toolchain + 6 assertions/discovery), in the order specified.
- Every `shell: pwsh` block begins with `Set-StrictMode -Version Latest` and `$ErrorActionPreference = 'Stop'`.
- MSC_VER cap derived from `${{ inputs.msvc_toolset }}` via `1900 + minor` formula — no hardcoded `1939` as a numeric literal.
- No `Jimver/cuda-toolkit`, no `microsoft/setup-msbuild` (toolset-selection anti-pattern), no `continue-on-error: true`, no `-allow-unsupported-compiler`.
- Every assertion step has a unique `id:` matching the Plan 03 consumption contract.
- Default-input dispatch is green on both jobs.
- Negative-input (`msvc_toolset=14.01`) dispatch is red at the MSVC probe step with a named-step error and a remediation hint.
- Upstream `build-wheels-cuda.yaml` is untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-toolchain-pinning/01-02-SUMMARY.md` capturing:
- Whether the default-input dispatch hit any Open-Question answers (especially OQ1: is `Microsoft.VisualStudio.Component.VC.14.39.17.9.x86.x64` actually installable on today's `windows-2022` image?). Record outcome: INSTALLABLE / NOT-INSTALLABLE (and if the latter, which remediation path was taken).
- Actual runner minutes consumed by the happy-path dispatch (budget is &lt;10 per RESEARCH.md — log actual for Phase 2 capacity planning).
- Exact wording of any remediation hints that fired during the negative test (so Plan 03 can cross-reference them in the DOC-04 comments).
- The resolved step IDs and their output names (so Plan 03's forensics summary doesn't re-derive them).
- Any deviations from the Plan's interfaces contract.
</output>
