---
phase: 03-smoke-test-publish-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/build-wheels-cuda-windows.yaml
  - llm_for_ci_test/tiny-llama.gguf
autonomous: false
requirements: [ST-01, ST-02, ST-03, ST-04, ST-05, ST-06, ST-07, ST-08]

must_haves:
  truths:
    - "smoke-test job runs on a FRESH windows-2022 runner (not the build runner) after build succeeds"
    - "llama_cpp/ source is excluded from the smoke-test checkout, so import resolves to site-packages"
    - "The built wheel installs successfully in a clean venv and from llama_cpp import Llama works"
    - "Llama loads tiny-llama.gguf and generates 2 tokens without segfault/access violation"
    - "cuobjdump --list-elf shows sm_80, sm_86, sm_89, sm_90 in the shipped ggml-cuda.dll"
    - "A segfaulting wheel would cause the smoke-test job to go red (WER suppressed, subprocess isolation)"
  artifacts:
    - path: "llm_for_ci_test/tiny-llama.gguf"
      provides: "27 KB GGUF fixture for inference smoke test"
      contains: "GGUF binary"
    - path: ".github/workflows/build-wheels-cuda-windows.yaml"
      provides: "smoke-test job definition with sparse checkout, venv, inference, cuobjdump, forensics"
      contains: "smoke-test:"
  key_links:
    - from: "smoke-test job"
      to: "build job"
      via: "needs: [build] + actions/download-artifact@v4 name: cuda-wheel"
      pattern: "needs:\\s*\\[build\\]"
    - from: "smoke-test venv python"
      to: "site-packages/llama_cpp/"
      via: "pip install wheel -> import llama_cpp resolves to venv site-packages"
      pattern: "site-packages"
    - from: "cuobjdump --list-elf"
      to: "site-packages/llama_cpp/lib/ggml-cuda.dll"
      via: "CUDA architecture verification on installed DLL"
      pattern: "cuobjdump.*list-elf"

user_setup: []
---

<objective>
Add the smoke-test job to the existing workflow and commit the GGUF fixture, creating the hard publish gate that prevents shipping wheels that compile but crash at runtime.

Purpose: The smoke-test job is the reason this fork exists -- upstream shipped segfaulting wheels because there was no runtime verification gate. This plan adds a separate job on a fresh runner that installs the built wheel into a clean venv and proves it can load a model and run inference without crashing. Phase 4's publish job will declare `needs: [build, smoke-test]` to complete the structural gate.

Output: Modified workflow YAML with smoke-test job, committed GGUF fixture, dispatch-verified green run.
</objective>

<execution_context>
@C:/Users/oliver/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oliver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-smoke-test-publish-gate/03-CONTEXT.md
@.planning/phases/03-smoke-test-publish-gate/03-RESEARCH.md
@.planning/phases/02-build-cache/02-02-SUMMARY.md

<interfaces>
<!-- Key contracts from the build job that the smoke-test job consumes -->

From .github/workflows/build-wheels-cuda-windows.yaml (build job outputs):
- Artifact name: `cuda-wheel` (uploaded via actions/upload-artifact@v4, compression-level: 0)
- Wheel filename pattern: `llama_cpp_python-<ver>+cu126.ll<sha12>-cp311-cp311-win_amd64.whl`
- Job ID: `build` (renamed from preflight in Phase 2)
- Runner: `windows-2022`

From .github/workflows/build-wheels-cuda-windows.yaml (lint-workflow job):
- Runs on: `ubuntu-latest`
- Steps: ban-grep (compiler-bypass flag), actionlint
- Phase 4 will add `needs: [build, smoke-test]` grep-assert here

From .github/workflows/build-wheels-cuda-windows.yaml (dispatch inputs):
```yaml
inputs.python_version: choice, default '3.11'
inputs.cuda_version: choice, default '12.6.3'
inputs.msvc_toolset: string, default 'auto'
inputs.verbose: boolean, default false
inputs.dry_run: boolean, default false
```

Established conventions:
- All Windows steps use `shell: pwsh` with `Set-StrictMode -Version Latest; $ErrorActionPreference = 'Stop'`
- External actions pinned with `@v4`/`@v5` semantic tags
- Hard-fail with remediation hints on assertion failure
- `if: always()` for cache saves
- `if: success()` for green forensics summary, `if: failure()` for red diagnostics
- Mamba steps use `shell: pwsh` (not bash -- Git Bash corrupts conda channel args)

DLL path after wheel install: `<venv>/Lib/site-packages/llama_cpp/lib/ggml-cuda.dll`
Expected CUDA architectures: sm_80, sm_86, sm_89, sm_90 (from build config `80-real;86-real;89-real;90-real;90-virtual`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit GGUF fixture and add smoke-test job to workflow</name>
  <files>llm_for_ci_test/tiny-llama.gguf, .github/workflows/build-wheels-cuda-windows.yaml</files>
  <action>
**Part A: Commit the GGUF fixture (ST-01)**

`git add llm_for_ci_test/tiny-llama.gguf` -- the file already exists locally (27488 bytes, untracked). Do NOT add the `llm_for_ci_test/Readme.md` -- only the `.gguf` file is needed for CI. Verify after add: `git ls-files llm_for_ci_test/tiny-llama.gguf` returns the path.

**Part B: Add `smoke-test` job to `.github/workflows/build-wheels-cuda-windows.yaml` (ST-02 through ST-07)**

Add a new job `smoke-test` between the `build` job and the `lint-workflow` job. The job structure follows the architecture pattern from RESEARCH.md (03-RESEARCH.md "Smoke-Test Job Structure" section). Key implementation details:

**Job-level config:**
- `needs: [build]` -- runs after build succeeds (ST-02)
- `runs-on: windows-2022` -- fresh runner (ST-02)
- `timeout-minutes: 30` -- no compilation; just install + test
- `defaults: run: shell: pwsh` -- match existing convention

**Steps (in order):**

1. **Sparse checkout (ST-03):** Use `actions/checkout@v4` with `sparse-checkout-cone-mode: false` and negation pattern to exclude `llama_cpp/` source. Exact config:
   ```yaml
   - name: 'Checkout (sparse -- exclude llama_cpp/ source)'
     uses: actions/checkout@v4
     timeout-minutes: 5
     with:
       sparse-checkout: |
         /*
         !llama_cpp/
       sparse-checkout-cone-mode: false
       fetch-depth: 1
       # NO submodules: recursive -- smoke-test doesn't need vendor source
   ```

2. **Download wheel artifact (ST-04):** Use `actions/download-artifact@v4` with `name: cuda-wheel` (contract from Phase 2). Set `timeout-minutes: 5`.

3. **Setup Python:** Use `actions/setup-python@v5` with `python-version: ${{ inputs.python_version }}`. Set `timeout-minutes: 5`.

4. **Setup mamba + install cuda-toolkit (for cuobjdump):** Reuse the same `conda-incubator/setup-miniconda@v3.1.0` pattern as the build job -- `miniforge-version: latest`, `activate-environment: smoke-test` (different env name from build job's `llamacpp`), `python-version: ${{ inputs.python_version }}`. Then a separate step: `mamba install -y --channel-priority strict --override-channels --channel nvidia/label/cuda-${{ inputs.cuda_version }} cuda-toolkit=${{ inputs.cuda_version }}`. Use `shell: pwsh` for mamba steps. Set `timeout-minutes: 15`.

5. **Disable WER crash dialog:** Set registry keys BEFORE any subprocess that might crash. Use the exact WER suppression code from RESEARCH.md (Pattern 3: WER Crash Dialog Suppression). Both `DontShowUI=1` and `Disabled=1` under `HKLM:\SOFTWARE\Microsoft\Windows\Windows Error Reporting`, plus `SetErrorMode(SEM_FAILCRITICALERRORS | SEM_NOGPFAULTERRORBOX)` via P/Invoke. Set `timeout-minutes: 2`.

6. **Assert sparse checkout exclusion (ST-03 proof):** Assert `Test-Path 'llama_cpp/__init__.py'` is FALSE (source excluded). Assert `Test-Path 'llm_for_ci_test/tiny-llama.gguf'` is TRUE (fixture present). Emit "OK" messages. Set `timeout-minutes: 2`.

7. **Create venv + install wheel (ST-05):** Create clean venv with `python -m venv .smoke-venv`. Use the venv's python explicitly: `$venvPython = Join-Path (Resolve-Path '.smoke-venv') 'Scripts\python.exe'`. Find the downloaded wheel with `Get-ChildItem *.whl` (download-artifact puts it in the workspace root). Install with `& $venvPython -m pip install $wheel`. Assert `$LASTEXITCODE -eq 0`. Set `timeout-minutes: 10`. Set step id `install-wheel` and emit output `wheel_name` (the filename of the installed wheel).

8. **Assert import resolves to site-packages (ST-03 continued):** Use venv python to run `import llama_cpp; print(llama_cpp.__file__)`. Assert output contains `site-packages`. Assert output does NOT start with the repo checkout path `(Get-Location).Path`. Set `timeout-minutes: 2`.

9. **Assert version cross-check:** Use venv python to run `import llama_cpp; print(llama_cpp.__version__)`. Assert version matches regex `\+cu126\.ll[a-f0-9]+` (proves the version embed from Phase 2 survived installation). Set `timeout-minutes: 2`.

10. **cuobjdump CUDA architecture check (ST-07):** Locate the DLL at `<venv>/Lib/site-packages/llama_cpp/lib/ggml-cuda.dll` (derive site-packages path from venv python: `import site; print(site.getsitepackages()[0])`). Run `cuobjdump --list-elf $dllPath`. Assert output contains all four expected architectures: `sm_80`, `sm_86`, `sm_89`, `sm_90`. Collect missing architectures and throw with remediation hint if any are absent. Set `timeout-minutes: 5`. Set step id `cuobjdump-check` and emit output `arch_list` (comma-separated list of found architectures).

11. **Inference test (ST-06):** Run in subprocess for crash safety. Use venv python to execute:
    ```python
    import sys
    from llama_cpp import Llama
    llm = Llama(model_path='llm_for_ci_test/tiny-llama.gguf', n_ctx=64, n_gpu_layers=0)
    output = llm('hi', max_tokens=2)
    text = output['choices'][0]['text']
    print(f'INFERENCE_OUTPUT={text}')
    sys.exit(0)
    ```
    Use `n_gpu_layers=0` to force CPU fallback (runner has no GPU). Capture output and check `$LASTEXITCODE`. Throw with the captured output on non-zero exit. On success, parse the `INFERENCE_OUTPUT=` line and save it for forensics. Set `timeout-minutes: 5`. Set step id `inference-test` and emit outputs `exit_code` and `output_tokens`.

12. **Green forensics summary (if: success()):** Emit a `$GITHUB_STEP_SUMMARY` table matching Phase 1's pattern. Include columns: Wheel filename, Installed version, Import path (`llama_cpp.__file__`), cuobjdump architectures, Venv Python path, Inference exit code, Output tokens (the 2-token gibberish), Python version, CUDA version. Set `timeout-minutes: 2`.

13. **Red diagnostics (if: failure()):** Emit `$GITHUB_STEP_SUMMARY` with detailed diagnostics: `pip list` from the venv, `(Get-Command python).Source` for both system and venv, DLL search path (`$env:PATH`), `llama_cpp.__file__` resolution attempt (may fail), `ggml-cuda.dll` location search (`Get-ChildItem -Recurse -Filter ggml-cuda.dll` in site-packages), cuobjdump output if available, and exception traceback. Use `$ErrorActionPreference = 'SilentlyContinue'` so the diagnostics step itself does not fail. Set `timeout-minutes: 5`.

14. **Restore mamba pkgs cache (save):** Use `actions/cache/save@v4` with `if: always()`. Key: use a separate prefix from build job -- `mamba-smoke-${{ inputs.cuda_version }}-${{ runner.os }}-${{ hashFiles('.github/workflows/build-wheels-cuda-windows.yaml') }}`. Path: `${{ env.MAMBA_ROOT_PREFIX }}/pkgs`. Also add a corresponding `actions/cache/restore@v4` step BEFORE the mamba install step (insert as step 4a, before `Install CUDA toolkit`), with the same key and restore-keys `mamba-smoke-${{ inputs.cuda_version }}-${{ runner.os }}-`.

**ST-08 handling:** The `needs: [build, smoke-test]` grep-assert on the publish job is DEFERRED to Phase 4 per CONTEXT.md locked decision. Phase 3 adds the smoke-test job; Phase 4 adds the publish job + gate assertion. ST-08 is satisfied by the fact that the smoke-test job's `needs: [build]` creates the dependency, and Phase 4 will add `needs: [build, smoke-test]` on the publish job (which this plan prepares for but does not implement).

**YAML placement:** The `smoke-test:` job block goes AFTER the `build:` job block and BEFORE the `lint-workflow:` job block. Update the ROADMAP comment in the workflow header to note Phase 3 is now implemented.

**Conventions to follow:**
- Every step gets `timeout-minutes`
- All pwsh steps start with `Set-StrictMode -Version Latest; $ErrorActionPreference = 'Stop'`
- Remediation hints on assertion failures (matching Phase 1/2 pattern)
- Use `shell: pwsh` for all Windows steps (including mamba steps -- Git Bash corrupts conda channel args)

Run `actionlint` locally after modifying the YAML to catch expression typos before committing.
  </action>
  <verify>
    <automated>cd C:/claude_checkouts/llama-cpp-python && git ls-files llm_for_ci_test/tiny-llama.gguf | grep -q tiny-llama.gguf && python -c "import yaml; y=yaml.safe_load(open('.github/workflows/build-wheels-cuda-windows.yaml')); jobs=list(y['jobs'].keys()); assert 'smoke-test' in jobs, f'smoke-test job missing, found: {jobs}'; st=y['jobs']['smoke-test']; assert 'build' in st.get('needs',[]), f'needs missing build'; assert st['runs-on']=='windows-2022'; print('OK: smoke-test job present, needs build, runs-on windows-2022')"</automated>
  </verify>
  <done>
    - `llm_for_ci_test/tiny-llama.gguf` is tracked in git (27488 bytes)
    - `smoke-test` job exists in the workflow YAML with `needs: [build]` and `runs-on: windows-2022`
    - Sparse checkout excludes `llama_cpp/` with assertion steps
    - Wheel is downloaded via `download-artifact@v4` with `name: cuda-wheel`
    - Clean venv created, wheel pip installed, import resolution asserted against site-packages
    - Version cross-check asserts `+cu126.ll<sha>` pattern
    - cuobjdump step verifies sm_80, sm_86, sm_89, sm_90 in ggml-cuda.dll
    - Inference test runs Llama in subprocess with WER suppressed and n_gpu_layers=0
    - Green forensics summary and red diagnostics steps present
    - Mamba cache save/restore with separate `mamba-smoke-*` key prefix
    - actionlint passes on the modified YAML
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Dispatch and verify smoke-test job end-to-end</name>
  <files>.github/workflows/build-wheels-cuda-windows.yaml</files>
  <action>
Dispatch the workflow via `gh workflow run build-wheels-cuda-windows.yaml` and verify the smoke-test job passes all assertions on a real GitHub Actions runner. This is the empirical validation step -- the smoke-test job exercises the full pipeline (sparse checkout, venv install, import resolution, version check, cuobjdump arch verification, inference test) against a real wheel built by the build job.
  </action>
  <verify>
    <automated>gh run list --workflow=build-wheels-cuda-windows.yaml --limit=1 --json status,conclusion | python -c "import sys,json; r=json.load(sys.stdin); print(r); assert r[0]['conclusion']=='success', f'Last run not successful: {r[0]}'"</automated>
  </verify>
  <done>
    - Dispatched workflow run shows three jobs: build, smoke-test, lint-workflow
    - smoke-test job is green with all assertion steps passing
    - Green forensics summary table visible in the Actions UI Summary tab
    - lint-workflow still passes (no regressions)
  </done>
</task>

</tasks>

<verification>
Phase 3 verification (after dispatch):
1. `smoke-test` job exists and runs on fresh `windows-2022` runner
2. Job depends on `build` (confirmed by `needs: [build]`)
3. Sparse checkout excludes `llama_cpp/` source (assertion step green)
4. Wheel artifact downloaded from build job (`cuda-wheel`)
5. Clean venv + pip install succeeds
6. Import resolves to `site-packages`, not repo checkout
7. Installed version matches `+cu126.ll<sha>` pattern
8. cuobjdump shows all 4 expected CUDA architectures
9. Inference test exits 0, producing 2 tokens
10. Green forensics summary renders correctly
11. Red forensics renders on failure (verified if any dispatch fails)
12. ST-08 (publish gate) prepared for Phase 4 -- smoke-test job exists for publish job to depend on
</verification>

<success_criteria>
- A dispatched workflow run shows three jobs: `build`, `smoke-test`, and `lint-workflow`
- The `smoke-test` job runs on a fresh runner after `build` completes
- All assertion steps (sparse checkout, import path, version, cuobjdump, inference) pass green
- The smoke-test job's forensics summary table is visible in the Actions UI Summary tab
- `lint-workflow` continues to pass (no regressions from new job)
</success_criteria>

<output>
After completion, create `.planning/phases/03-smoke-test-publish-gate/03-01-SUMMARY.md`
</output>
